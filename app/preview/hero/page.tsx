// PREVIEW-ONLY: the depth-drift hero (handoff "Bulletin Hero — drifting
// cards", 2026-09-01) merged with the live landing's lockup + dot grid and
// the v3 hero's real link cards. Swap onto `/` when called done. Safe to
// delete.
import { DepthHero } from '@/components/home/DepthHero'

export default function HeroPreview() {
  return <DepthHero />
}
