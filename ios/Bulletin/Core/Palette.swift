import SwiftUI

// Editorial palette tokens (mirror of the web tailwind.config) — in Core so
// the share extension gets them too.
extension Color {
    static let ink = Color(red: 0x2b / 255, green: 0x2b / 255, blue: 0x2b / 255)
    static let cardGrey = Color(red: 0xf1 / 255, green: 0xf1 / 255, blue: 0xf1 / 255)
}
