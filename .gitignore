# dependencies
node_modules/

# next.js
.next/
out/

# env files
.env
.env.local
.env*.local

# debug
npm-debug.log*

# misc
.DS_Store
*.pem
tsconfig.tsbuildinfo
.claude/
.vercel

# build bundles (extension zips etc.) — keep binary blobs out of the repo
*.zip

# local seeding tooling — kept off the public repo (scripts/harvest/out = scraped data)
/scripts/harvest/
